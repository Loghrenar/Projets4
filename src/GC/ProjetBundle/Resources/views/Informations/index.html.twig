{% extends 'GCProjetBundle::layoutt.html.twig' %}

{% block body -%}
    <h1>Informations list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Infodesc</th>
                <th>Infonom</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gc_informations_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.infoDesc }}</td>
                <td>{{ entity.infoNom }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gc_informations_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gc_informations_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gc_informations_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
